# Implementation Plan

> 当前仅规划；不得在本阶段修改产品代码或启动任务。

1. [x] **三态回归先行** [R1]：在 `src-tauri/src/services/usage/fs_backend.rs` tests 与 `src-tauri/src/services/usage/providers/*.rs` tests 添加 missing/empty/success/transport/protocol/permission fixtures，先复现 remote error 被压成 false/empty。定向验证：`cargo test --manifest-path src-tauri/Cargo.toml --locked services::usage::fs_backend`。Rollback: RP1。
2. [x] **Typed error 与 backend propagation** [R1][R3][R5]：修改 `src-tauri/src/services/usage/error.rs::UsageError`、`src-tauri/src/services/usage/fs_backend.rs::FsBackend` 和 Local/Remote 实现；保留内部 source，公开稳定 code/retryable/redacted message。定向验证：步骤 1 + `cargo test --manifest-path src-tauri/Cargo.toml --locked services::usage::error`。Rollback: RP1。
3. [x] **Provider availability migration** [R1][R6]：逐个修改 `src-tauri/src/services/usage/providers/*.rs::available` 与调用点为 `Result<bool, UsageError>`；stub 仍 `Ok(false)`，Local missing 仍非错误。定向验证：`cargo test --manifest-path src-tauri/Cargo.toml --locked services::usage::providers` 和 `cargo check --manifest-path src-tauri/Cargo.toml --all-targets --locked`。Rollback: RP1。
4. [x] **Target commit gate** [R2][R3]：在 `src-tauri/src/services/usage/mod.rs::refresh_with_providers` 聚合 target-fatal error，并在 enrichment/file-cache/`replace_calls_for_target` 前返回；保留本地可容错 provider outcome。定向验证：`cargo test --manifest-path src-tauri/Cargo.toml --locked refresh_marks_available_provider_unavailable_when_collect_fails` 与新增 remote-fatal preservation tests。Rollback: RP2。
5. [x] **逐表保全与合法空提交** [R2][R4]：在 `src-tauri/src/services/usage/tests.rs` 和 `src-tauri/src/db/repos/usage_repo.rs` tests 预置 calls/providers/metadata/scan/file-cache，断言失败零写入、合法空可清空、失败 target 不影响另一个 target、成功 target summary 真实。定向验证：`cargo test --manifest-path src-tauri/Cargo.toml --locked services::usage` 和 `cargo test --manifest-path src-tauri/Cargo.toml --locked db::repos::usage_repo`。Rollback: RP2。
6. [x] **Redaction 回归** [R3][R5]：捕获 tracing/IPC error，注入含 path/command/stderr/host 的底层 source，断言输出仅 target-safe ID/provider/code/retryable。定向验证：`cargo test --manifest-path src-tauri/Cargo.toml --locked usage_remote` 与 `cargo test --manifest-path src-tauri/Cargo.toml --locked ipc_error::redaction_contract_tests`。Rollback: RP3。
7. [x] **总验证** [R1-R6]：运行 `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check`、`cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --locked -- -D warnings`、`cargo test --manifest-path src-tauri/Cargo.toml --all-targets --locked`、默认并发 `just ci`、`git diff --check` 和任务验证；确认无 schema/docs/DTO 漂移。
8. [x] **真实远程边界与审查** [R4][R5]：若有受控目标，分别执行 SSH/WSL missing、permission denied、断连 smoke，并核对旧 rows；否则逐项标 `UNVERIFIED`。回滚只恢复本任务代码，不删除旧 usage rows，不手工改 scan timestamp；独立 review 核对 commit gate 位于所有 DB/cache mutation 之前。
