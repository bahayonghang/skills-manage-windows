# Implementation Plan

> Goal 授权覆盖规划冻结；实施已完成。真实 SSH/WSL/kill/commit-unknown 保持 UNVERIFIED。

1. [x] **冻结故障矩阵** [R2-R4][R7][R8]：在 `src-tauri/src/services/central_updates/core/content_upsert.rs`、`src-tauri/src/services/central_updates/core/batch_tests.rs`、`src-tauri/src/services/central_updates/fs/operation/tests.rs` 先增加 prepared/staged/swapped/DB apply/commit-visible/commit-invisible/finalize/marker/fingerprint fixtures，并断言 exact phase、row、old/new FS、business DB。定向验证：`cargo test --manifest-path src-tauri/Cargo.toml --locked services::central_updates::core::content_upsert` 和 `cargo test --manifest-path src-tauri/Cargo.toml --locked services::central_updates::core::batch::tests`。Rollback: RP1。
2. [x] **补 canonical seam 输入契约** [R2][R3][R6]：在 `src-tauri/src/services/central_updates/core/content_upsert.rs::{JournaledCentralContentUpsert,content_upsert_plan}` 明确 first/overwrite 的 existing skill、uid、`had_target`、target identity、commit/digest；不得改 phase graph。定向验证：重复步骤 1，并运行 `cargo test --manifest-path src-tauri/Cargo.toml --locked db::repos::fs_db_operations_repo`。Rollback: RP1。
3. [x] **Local 调用路径收敛** [R1][R5-R7]：修改 `src-tauri/src/services/github_import/import.rs::import_single_staged_skill`，让 snapshot/frontmatter validation 在锁前完成，最终 apply 只调用 `journaled_central_content_upsert`；DB/rollback/finalize 错误沿 canonical stable phase/code 返回。定向验证：`cargo test --manifest-path src-tauri/Cargo.toml --locked services::github_import` 与 content-upsert tests。Rollback: RP2。
4. [x] **SSH/WSL 调用路径收敛** [R1][R5-R7]：修改 `src-tauri/src/services/github_import/remote.rs::import_github_repo_skills_remote_from_workspace`，remote workspace 只提供已验证 bytes，最终 apply 交给 `CentralFs`/batch；移除脚本内 target backup 删除。定向验证：`cargo test --manifest-path src-tauri/Cargo.toml --locked marketplace_content_upsert_completes_full_saga_for_fake_ssh_and_wsl`，再运行 remote import FakeRunner tests。Rollback: RP3。
5. [x] **恢复入口与并发** [R4][R5][R8]：在 `src-tauri/src/services/central_operation/recovery.rs`、`src-tauri/src/services/central_updates/core/batch/recovery.rs` 和相关 tests 证明 Local `src-tauri/src/lib.rs::install_ready_state` 只恢复 Local，SSH/WSL `retry_fs_db_operation`/下一 mutation 使用匹配 transport；添加 same-target import×delete/update/install 串行和 different-target 并行测试。定向验证：`cargo test --manifest-path src-tauri/Cargo.toml --locked services::central_operation::recovery` 和 `cargo test --manifest-path src-tauri/Cargo.toml --locked central_mutation`。Rollback: RP3。
6. [x] **删除平行 apply/dead helper** [R1]：确认无生产 call 后删除 `src-tauri/src/services/github_import/import.rs::{backup_existing_skill_dir,restore_or_cleanup_target_dir,drop_existing_backup}` 与 `src-tauri/src/services/github_import/remote.rs::remote_import_skill_script` 的最终 apply 职责；保留 preview workspace cleanup。定向验证：`rtk rg -n "upsert_skill_with_github_repository|remote_import_skill_script|restore_or_cleanup_target_dir" src-tauri/src/services/github_import`，预期仅允许非 apply/test 明确项。Rollback: RP4。
7. [x] **Redaction 与 payload 回归** [R3][R6][R7]：补 IPC/operation/runtime log 捕获断言和 multi-file byte-equivalence、uid、summary/progress tests。定向验证：`cargo test --manifest-path src-tauri/Cargo.toml --locked github_import` 和 `cargo test --manifest-path src-tauri/Cargo.toml --locked ipc_error::redaction_contract_tests`。Rollback: 与对应 Local/remote 批次一起回退。
8. [x] **总验证** [R1-R8]：运行 `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check`、`cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --locked -- -D warnings`、`cargo test --manifest-path src-tauri/Cargo.toml --all-targets --locked`、默认并发 `just ci`、`git diff --check` 和任务验证；独立 review 逐行对照 failure matrix。
9. [x] **真实远程边界** [R8]：若有受控环境，执行 ignored WSL smoke、SSH 断连/进程 kill/显式 Retry；否则将真实 SSH、WSL、kill、commit-unknown 分别标为 `UNVERIFIED`。任何真实测试遗留 pending row/artifact 时按 operation ID/manifest owner 回滚，禁止手工 broad delete。
