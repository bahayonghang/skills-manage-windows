# Product

## Register

product

## Users

主要服务**独立开发者 / 重度用户**：他们同时使用多个 AI 编程 agent（Claude Code、Cursor、Codex、Gemini 等），手里积累了大量 SKILL.md 技能，痛点是「技能散落在各平台目录、版本不一、装卸全靠手动 symlink」。

他们使用本工具时的语境：在桌面端，把中央目录当作技能的唯一真源，频繁地在「中央库 ↔ 各平台 ↔ 项目 ↔ 市场」之间浏览、安装、卸载、pin、分组。他们要做的事是**集中掌控**——一眼看清每个技能装在哪些平台，一次操作分发到位。因此偏好高信息密度与键盘优先，而非被引导式的简化界面。

## Product Purpose

SkillPort 是一个跨平台 AI 技能管理桌面应用（Tauri + 本地 SQLite，本地优先）。它以 `~/.agents/skills/` 中央目录为技能的唯一真实来源，通过符号链接把技能安装到各 agent 平台目录，并提供技能市场、集合、项目级管理、使用统计等能力。

存在的理由：AI 编程工具碎片化，技能资产没有统一的家。成功的样子是——用户不再手动管理 symlink 和散落的 SKILL.md，打开应用就能看清全局安装状态、按需一键分发，且界面状态始终如实反映文件系统真相。

## Brand Personality

**鲜明 · 可玩 · 可主题化**，但扎根于专业工艺。

个性来自主题系统本身：Catppuccin 4 风味 + Claude 亮/暗共 6 套主题、14 种 accent 可切换——配色即身份，呼应 Catppuccin 生态 / Ghostty / Obsidian / Zed 这类「有个性但不牺牲密度与性能」的开发者工具气质。voice 是自信、克制、技术感，body 默认等宽字体（JetBrains Mono）强化终端原生味。

关键张力：表达个性靠**主题与 accent 的一致运用**，绝不靠气泡化装饰。可玩 ≠ 玩具感；鲜明 ≠ 喧闹。工具要消失在任务里（Linear/Raycast 那条「earned familiarity」基准线）。

## Anti-references

这个界面绝对不能像：

- **千篇一律的 SaaS 仪表盘**——渐变大数字、hero-metric 模板、无差别的图标+标题+文字卡片网格。
- **臃肿的企业级后台**——工具栏塞满、层级混乱、信息没有优先级。
- **玩具感消费应用**——过度圆角、气泡化、缺乏专业严肃感。
- **AI 生成的通用感**——一眼能看出是套模板、缺乏自身识别度。

## Design Principles

1. **工具消失在任务里**：重度用户每天住在这里，界面要让「技能装在哪、装没装」一目了然，而不是让人欣赏装饰。标准任务（装/卸/pin/搜索/分组）用标准范式，不为花样重造轮子。
2. **个性靠主题，不靠装饰**：鲜明与可玩通过 Catppuccin/Claude 主题与 accent 的一致运用来表达；任何一处都不允许用气泡化、玩具化的临时装饰来「增加趣味」。这条直接化解「鲜明可玩」与「反玩具感」的张力。
3. **本地优先的诚实**：它操作的是真实文件（symlink、中央目录、SQLite）。界面展示的安装/链接状态必须与文件系统真相一致，不做乐观假象；失败、跳过、冲突要如实呈现。
4. **密度但有层级**：重度用户要的是信息量，但每个密集界面仍需清晰的视觉层级与节奏（这是与「臃肿企业后台」的分界线）。
5. **一致优于惊喜**：同一种按钮形状、同一套表单控件、同一种图标风格贯穿全应用；delight 留给关键时刻，而非每个页面。

## Accessibility & Inclusion

- 基线 **WCAG 2.1 AA**：正文对比度 ≥4.5:1，大字 ≥3:1；占位符文本同样需达 4.5:1。
- **完整键盘可达**：所有交互（装卸/pin/搜索/切换主题/平台图标切换）可纯键盘完成，焦点态清晰可见（沿用 `--ring` token）。
- **减动效**：尊重 `prefers-reduced-motion`，已在 startup-shell 等处部分处理，需在所有动画上保证降级（淡入或瞬切）。
- **色弱友好**：14 种 accent 与 chart-1..5 配色不能成为唯一信息载体——状态/图表除色相外，需辅以图标、文字或形状区分。
